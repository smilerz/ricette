{
  "autoMode": {
    "environment": [
      "$defaults",
      "Trusted repo boundary: /home/smilerz/ricette only. Do not import or inherit permissions from sibling projects on this machine (e.g. WeekFare/Tandoor) -- this policy is scoped to this repo alone.",
      "Repository state as of 2026-08-29: local, git-less, will become a public GitHub repo (Laravel 13 + Svelte 5 + Inertia 3, per the Foundation 0 governance doc). No remote configured yet.",
      "No cloud provider, no deployment target, no secrets manager, no internal services, and no package/container registry publication are configured yet -- do not invent placeholder config for these.",
      "RE-BASELINE TRIGGER: this policy should be revisited the first time any of the following occurs -- a remote is added or visibility changes, a hosted environment or cloud provider is introduced, secrets are added, a deployment pipeline is added, package/container publication is configured, a new repository becomes trusted, or a production environment is established."
    ],
    "allow": [
      "$defaults",
      "Branch creation, file edits, formatting, and running tests inside /home/smilerz/ricette",
      "Local container builds and runs (e.g. docker build/run) for development and testing",
      "Installing dependencies from approved public registries only: Packagist/Composer, the npm/pnpm registry, GitHub source/releases, and official container base images (e.g. Docker Hub official images)",
      "git commit, and git push to feature branches only -- never to main or other protected branches",
      "gh pr create / gh pr update -- opening and updating pull requests"
    ],
    "soft_deny": [
      "$defaults",
      "Edits to governance/security/control-plane material: repository rules / branch protection config, CI security configuration, CODEOWNERS, .claude/**, AGENTS.md, SECURITY.md, LICENSE*, docs/adr/**, dependency policy docs. These may still be edited freely on a branch -- a branch edit carries no authority until merged through its protected CODEOWNER review path -- but flag such changes conspicuously rather than treating them as routine."
    ],
    "_hard_deny_note": "Hard-deny authority-boundary invariants for this repo (no direct/force push to main, no self-merge, no bypassing required review/CI, no publishing outside the configured repo, no package/container/release publication before configured, no secret exfiltration, no destructive prod ops) are intentionally NOT defined in this file. They are enforced from ~/.claude/settings.json (user-level, outside this repo's own edit reach) so the repo being worked on cannot edit the rules constraining the work done in it. Do not re-add a hard_deny array here -- that would defeat the point of the split."
  }
}
