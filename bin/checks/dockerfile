#!/usr/bin/env bash
# Dockerfile lint (Foundation 0 §30).
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/../.."
# shellcheck source=bin/lib.sh
source bin/lib.sh

py_tool hadolint-py 2.15.1.2 hadolint Dockerfile
