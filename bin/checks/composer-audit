#!/usr/bin/env bash
# Known-vulnerability audit of PHP and JavaScript dependencies (Foundation 0 §35, §41).
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/../.."
# shellcheck source=bin/lib.sh
source bin/lib.sh

step composer-audit composer audit --locked
step pnpm-audit pnpm audit --audit-level=high
steps_result
