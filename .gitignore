# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp

# Environment / secrets
.env
.env.*
!.env.example

# PHP / Laravel (scaffolded in Phase 16)
/vendor/
/storage/*.key
/bootstrap/cache/*.php
/public/build/
/public/hot

# JS / frontend
/node_modules/
/dist/
/.pnpm-store/

# Database (SQLite dev files)
*.sqlite
*.sqlite-journal

# Claude Code state — local only, not shared/reviewed as project governance
# (reverses the earlier decision to commit .claude/settings.json; see
# IMPLEMENTATION_PLAN.md "Auto-mode enforcement split" for the history)
.claude/
